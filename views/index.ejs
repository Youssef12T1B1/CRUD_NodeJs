

    
        <div class="divider"></div>
 
        <div class="row mt-5">
            <div class="col-md-10 m-auto">

                <!-- <div class="section">
                    <a href="create-user" class="btn red darken-1">
                    <i class="fas fa-user left"></i> New User  
                    </a>
                </div> -->
                <div class="card card-body text-center">
                    <form action="" method="post">
                        <table class="table">
                            <thead class="thead">
                                <tr>
                                    <th>ID</th>
                                    <th>Username</th>
                                    <th>Email</th>
                                    <th>Gender</th>
                                    <th>Status</th>
                                    <th>Action</th>
                                </tr>

                            </thead>
                            <tbody>
                                <% for (var i=0 ; i< users.length;++i){%>
                                <tr>
                                    <td><%= i+1 %></td>
                                    <td><%= users[i].username %></td>
                                    <td><%= users[i].email  %></td>
                                    <td><%= users[i].gender %></td>
                                    <td><%= users[i].status  %></td>
                                    <td>

                                        <a href="/update-user?id=<%= users[i]._id  %>" class="btn border-shadow update"> <span class="text-gradient">
                                            <i class="fas fa-pencil-alt"></i>
                                        </span> </a>
                                        <a  class="btn border-shadow delete" data-id= <%= users[i]._id  %> > <span class="text-gradient">
                                            <i class="fas fa-times"></i>
                                        </span> </a>
                                    </td>
                                   
                                </tr>
                                <% } %>
                            </tbody>
                        </table>

                        


                    </form>
        </div>
        </div>
        </div>
        <div class="fixed-action-btn">


            <a href="create-user" class="btn-floating btn-large waves-effect waves-lightred"><i class="fas fa-plus"></i></a>
        </div>